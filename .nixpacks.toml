# .nixpacks.toml

[phases.setup]
# 🟢 Dùng Node 22 thay vì Node 20
nixPkgs = ["bash", "nodejs_22"]

[phases.caddy]
pkgs = ["caddy"]
cmds = ["caddy fmt --overwrite /assets/Caddyfile"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "caddy run --config /assets/Caddyfile --adapter caddyfile 2>&1"
